import React from 'react'
import { personName } from './Modal'

const Person = (props: personName) => {
    return (
        <div>{props.Property.Finame}</div>
        // <div></div>
    )
}

export default Person